const fs = require('fs');
const path = require('path');

const ROOT = path.resolve(__dirname, '..', '..');
const TASKS_FILE = path.join(__dirname, 'tasks.json');
const LOG_FILE = path.join(__dirname, 'logs', 'kairos.log');
const REPORT_FILE = path.join(__dirname, 'logs', 'kairos-report.json');
const JUNK_FILE = path.join(__dirname, 'logs', 'kairos-junk.json');

const TRASH_DIRNAME = '_KAIROS_TRASH';
const DISK_E_ROOT = 'E:\\';
const CLEAN_SKIP_DIRS = new Set(['.git', 'node_modules', TRASH_DIRNAME, '$RECYCLE.BIN', 'System Volume Information', 'Config.Msi', 'Recovery']);
const LIVE_DIRS = ['server', 'client', 'server/agents'].map(d => path.join(ROOT, d).toLowerCase());
const APP_INSTALL_RE = /^(cursor|microsoft vs code|vscode|nodejs|node-v|python\d*|git|obs-studio|vscode|androidstudio|jdk|jre)$/i;
const JUNK_FILE_RE = /(^|[\\/])(\\.DS_Store|Thumbs\\.db|desktop\\.ini)$|\\.(tmp|temp|swp|orig|rej|old|log~)$|~$/i;
const REPLICA_RE = /( - copia| copia \d+| copy\b| - copy|\(\d+\)\.|-old\b|_old\b|-backup\b|_backup\b|-viejo\b|_viejo\b|\\.bak$|\\.bak\\.)/i;
const JUNK_DIR_RE = /^(build|dist|coverage|\\.cache|\\.next|\\.parcel-cache|out|tmp|temp)$/i;
const PLACEHOLDER_RE = /^$|cambiame|tu_|tu-|xxxx|pk_test_$|sk-proj-tu|_aqui|direccion_del|example|changeme/i;
const DEV_FILE_RE = /(__tests__|\\.test\\.|seedData|final-validation|test-dependencies|check-status|generate-)/;

const INTEGRATIONS = [
  { id: 'mongodb', name: 'MongoDB (base de datos)', dep: 'mongoose', env: ['MONGODB_URI'], code: ['server/config/database.js'], critical: true },
  { id: 'auth-jwt', name: 'Autenticación JWT', dep: 'jsonwebtoken', env: ['JWT_SECRET'], code: ['server/routes/auth.js'], critical: true },
  { id: 'stripe', name: 'Pagos / Suscripciones (Stripe)', dep: 'stripe', env: ['STRIPE_SECRET_KEY', 'STRIPE_WEBHOOK_SECRET'], code: ['server/routes/checkout.js', 'server/routes/subscription.js'], critical: true },
  { id: 'cloudinary', name: 'Almacenamiento multimedia (Cloudinary)', dep: 'cloudinary', env: ['CLOUDINARY_CLOUD_NAME', 'CLOUDINARY_API_KEY', 'CLOUDINARY_API_SECRET'], code: ['server/routes/multimedia.js'], critical: true },
  { id: 'openai', name: 'IA generativa (OpenAI)', dep: 'openai', env: ['OPENAI_API_KEY'], code: ['server/services/aiService.js'], critical: false },
  { id: 'deepl', name: 'Traducción (DeepL)', dep: null, env: ['DEEPL_API_KEY'], code: ['server/routes/translation.js'], critical: false },
  { id: 'web3', name: 'Token KRO / Web3 (ethers + Infura)', dep: 'ethers', env: ['INFURA_API_KEY', 'SEPOLIA_RPC_URL', 'KRONOS_TOKEN_ADDRESS'], code: ['server/services/tokenService.js'], critical: false },
  { id: 'push', name: 'Notificaciones push (Web Push / VAPID)', dep: 'web-push', env: ['VAPID_PUBLIC_KEY', 'VAPID_PRIVATE_KEY', 'VAPID_SUBJECT'], code: ['server/services/pushService.js'], critical: false },
  { id: 'email', name: 'Email transaccional (Nodemailer)', dep: 'nodemailer', env: ['EMAIL_HOST', 'EMAIL_USER', 'EMAIL_PASSWORD'], code: ['server/services/emailService.js'], critical: false },
  { id: 'oauth', name: 'Login social (Google / Facebook OAuth)', dep: 'passport', env: ['GOOGLE_CLIENT_ID', 'GOOGLE_CLIENT_SECRET'], code: [], critical: false },
  { id: 'socketio', name: 'Comunicación en tiempo real (Socket.io)', dep: 'socket.io', env: ['REACT_APP_SOCKET_URL'], code: ['server/server.js'], critical: true },
];

function log(msg) {
  const line = `[${new Date().toISOString()}] [KAIROS] ${msg}`;
  console.log(line);
  try { fs.mkdirSync(path.dirname(LOG_FILE), { recursive: true }); fs.appendFileSync(LOG_FILE, line + '\n'); } catch {}
}

function read(rel) { const abs = path.join(ROOT, rel); try { return fs.readFileSync(abs, 'utf8'); } catch { return null; } }
function exists(rel) { return fs.existsSync(path.join(ROOT, rel)); }
function walk(relDir, exts) { const out = []; const base = path.join(ROOT, relDir); if (!fs.existsSync(base)) return out; const stack = [base]; while (stack.length) { const dir = stack.pop(); let entries = []; try { entries = fs.readdirSync(dir, { withFileTypes: true }); } catch { continue; } for (const e of entries) { if (e.name === 'node_modules' || e.name === '.git' || e.name === 'build') continue; const full = path.join(dir, e.name); if (e.isDirectory()) { stack.push(full); continue; } if (!exts || exts.some(x => e.name.endsWith(x))) out.push(path.relative(ROOT, full)); } } return out; }
function parseEnvKeys(content) { if (!content) return {}; const map = {}; for (const raw of content.split('\n')) { const line = raw.trim(); if (!line || line.startsWith('#')) continue; const eq = line.indexOf('='); if (eq === -1) continue; map[line.slice(0, eq).trim()] = line.slice(eq + 1).trim(); } return map; }
function parseRenderKeys(content) { if (!content) return []; return [...content.matchAll(/-\s*key:\s*([A-Z0-9_]+)/g)].map(m => m[1]); }
function collectEnvUsage() { const server = new Set(); const client = new Set(); const re = /process\.env\.([A-Z0-9_]+)/g; for (const f of walk('server', ['.js'])) { if (DEV_FILE_RE.test(f)) continue; const c = read(f); if (!c) continue; for (const m of c.matchAll(re)) server.add(m[1]); } for (const f of walk('client/src', ['.js', '.jsx'])) { const c = read(f); if (!c) continue; for (const m of c.matchAll(re)) client.add(m[1]); } return { server: [...server], client: [...client] }; }
function auditEnv() { const findings = []; const envExample = parseEnvKeys(read('.env.example')); const renderKeys = parseRenderKeys(read('render.yaml')); const usage = collectEnvUsage(); if (!exists('.env') && !exists('.env.local')) { findings.push({ sev: 'warn', area: 'env', msg: 'No se detectó un archivo .env local. Kairos recomienda crear uno basado en .env.example para que las integraciones funcionen localmente.', fixable: false }); }
 const exampleKeys = new Set(Object.keys(envExample)); const renderSet = new Set(renderKeys);
 for (const key of usage.server) { if (key === 'NODE_ENV' || key === 'PORT' || key === 'npm_package_version') continue; if (!exampleKeys.has(key)) { findings.push({ sev: 'warn', area: 'env', msg: `El servidor usa process.env.${key} pero NO está en .env.example (riesgo: se olvida al desplegar).`, fixable: true, fixKind: 'env-doc', key }); } if (!key.startsWith('REACT_APP_') && !renderSet.has(key)) { findings.push({ sev: 'warn', area: 'env', msg: `process.env.${key} no está declarada en render.yaml — Render no la inyectará en producción.`, fixable: false }); } }
 for (const [key, val] of Object.entries(envExample)) { const isSecret = /KEY|SECRET|TOKEN|URI|PASS|PASSWORD/i.test(key); if (isSecret && PLACEHOLDER_RE.test(val)) { findings.push({ sev: 'info', area: 'env', msg: `${key} en .env.example es un placeholder — recuerda poner el valor real en Render/Vercel (no en Git).`, fixable: false }); } }
 const emailExample = Object.keys(envExample).filter(k => /^SMTP_/.test(k)); const emailRender = renderKeys.filter(k => /^EMAIL_/.test(k)); if (emailExample.length && emailRender.length) { findings.push({ sev: 'error', area: 'env', msg: `Incoherencia de email: .env.example usa ${emailExample.join(', ')} pero render.yaml usa ${emailRender.join(', ')}. El código lee uno solo — unifica los nombres o el envío de correo fallará en producción.`, fixable: false }); }
 return findings; }
function auditRoutes() { const findings = []; const serverJs = read('server/server.js'); if (!serverJs) return findings; const routeFiles = walk('server/routes', ['.js']).map(f => path.basename(f, '.js')); for (const name of routeFiles) { const registered = new RegExp(`require\\(['"]\\./routes/${name}['"]\\)`).test(serverJs); if (!registered) { findings.push({ sev: 'error', area: 'routes', msg: `La ruta server/routes/${name}.js existe pero NO está registrada en server.js (endpoint muerto, inaccesible desde la web).`, fixable: true, fixKind: 'register-route', route: name }); } } return findings; }
function auditClientPages() { const findings = []; if (!exists('client/src/App.jsx')) return findings; const SKIP = new Set(['KronosMockups']); const allClient = walk('client/src', ['.js', '.jsx']); for (const f of walk('client/src/pages', ['.jsx'])) { const name = path.basename(f, '.jsx'); if (SKIP.has(name)) continue; const referenced = allClient.some(other => { if (other === f) return false; const c = read(other); return c && new RegExp(`\\b${name}\\b`).test(c); }); if (!referenced) { findings.push({ sev: 'warn', area: 'client', msg: `La página ${f} existe pero ningún otro archivo del cliente la referencia — probablemente no tiene <Route> y el usuario no puede llegar a ella.`, fixable: false }); } } return findings; }
function loadDeps() { const s = JSON.parse(read('server/package.json') || '{}'); const c = JSON.parse(read('client/package.json') || '{}'); return { server: { ...(s.dependencies || {}), ...(s.devDependencies || {}) }, client: { ...(c.dependencies || {}), ...(c.devDependencies || {}) }, }; }
function auditIntegrations() { const findings = []; const deps = loadDeps(); const envExample = parseEnvKeys(read('.env.example')); const renderKeys = new Set(parseRenderKeys(read('render.yaml'))); const declared = new Set([...Object.keys(envExample), ...renderKeys]); for (const intg of INTEGRATIONS) { const missing = []; if (intg.dep && !deps.server[intg.dep]) missing.push(`dependencia "${intg.dep}" no instalada`); for (const e of intg.env) if (!declared.has(e)) missing.push(`falta var ${e}`); for (const c of intg.code) if (!exists(c)) missing.push(`falta archivo ${c}`); if (missing.length) { const sev = intg.critical ? 'error' : 'warn'; findings.push({ sev, area: 'integration', intg: intg.id, msg: `${intg.name}: integración incompleta → ${missing.join('; ')}.`, fixable: false }); } } return findings; }
function auditDeploy() { const findings = []; if (!exists('render.yaml')) { findings.push({ sev: 'error', area: 'deploy', msg: 'Falta render.yaml — el backend no tiene definición de despliegue en Render.', fixable: false }); } else { const render = read('render.yaml'); const m = render.match(/healthCheckPath:\s*(\S+)/); if (m) { const serverJs = read('server/server.js') || ''; if (!serverJs.includes(m[1])) { findings.push({ sev: 'warn', area: 'deploy', msg: `healthCheckPath "${m[1]}" del render.yaml no parece existir en server.js — Render marcará el servicio como caído.`, fixable: false }); } } }
 if (!exists('vercel.json')) { findings.push({ sev: 'error', area: 'deploy', msg: 'Falta vercel.json — el frontend no tiene configuración de build/SPA en Vercel.', fixable: false }); }
 const clientPkg = JSON.parse(read('client/package.json') || '{}'); if (!clientPkg.scripts || !clientPkg.scripts.build) { findings.push({ sev: 'error', area: 'deploy', msg: 'El cliente no tiene script "build" — Vercel no podrá compilar el frontend.', fixable: false }); }
 const example = parseEnvKeys(read('.env.example')); if (example.REACT_APP_API_URL && /localhost/.test(example.REACT_APP_API_URL)) { findings.push({ sev: 'info', area: 'deploy', msg: 'REACT_APP_API_URL apunta a localhost en .env.example — en Vercel debe ser la URL de Render terminada en /api.', fixable: false }); }
 if (example.REACT_APP_SOCKET_URL) { if (example.REACT_APP_SOCKET_URL.endsWith('/')) { findings.push({ sev: 'warn', area: 'deploy', msg: 'REACT_APP_SOCKET_URL termina en "/". Socket.io a veces presenta problemas de handshake si la URL base incluye la barra final.', fixable: false }); } }
 return findings; }
function auditSecrets() { const findings = []; const files = [...walk('server', ['.js']), ...walk('client/src', ['.js', '.jsx'])]; for (const f of files) { if (DEV_FILE_RE.test(f)) continue; const c = read(f); if (!c) continue; for (const { re, what } of [{ re: /sk_(live|test)_[A-Za-z0-9]{10,}/, what: 'clave secreta de Stripe (sk_)' }, { re: /whsec_[A-Za-z0-9]{10,}/, what: 'secreto de webhook de Stripe (whsec_)' }, { re: /GOCSPX-[A-Za-z0-9_-]{10,}/, what: 'client secret de Google OAuth' }, { re: /AIza[0-9A-Za-z_-]{30,}/, what: 'API key de Google (AIza)' }, { re: /AKIA[0-9A-Z]{16}\b/, what: 'access key de AWS (AKIA)' }, { re: /mongodb(\+srv)?:\/\/[^
'"`]+:[^
'"`@]+@/, what: 'connection string de Mongo con usuario:contraseña' }, { re: /-----BEGIN (RSA |EC )?PRIVATE KEY-----/, what: 'llave privada' }]) { if (re.test(c)) { findings.push({ sev: 'error', area: 'security', msg: `${f}: parece tener un ${what} escrito directo en el código — muévelo a una variable de entorno (queda expuesto en Git).`, fixable: false }); } } } return findings; }
function auditRouteGuards() { const findings = []; for (const name of ['wallet', 'admin', 'refunds', 'twofactor', 'tips', 'subscription']) { const rel = `server/routes/${name}.js`; const c = read(rel); if (!c) continue; const hasRoutes = /router\.(get|post|put|patch|delete)\s*\(/.test(c); if (hasRoutes && !/protect|requireAdmin|adminAuth|web3Auth|authMiddleware|verifyToken|isAuthenticated/.test(c)) { findings.push({ sev: 'error', area: 'security', msg: `${rel}: ruta sensible SIN middleware de autenticación — cualquiera podría usarla sin iniciar sesión. Agrega "protect" (o el guard que corresponda).`, fixable: false }); } } return findings; }
function auditPayments() { const findings = []; const plans = read('server/config/subscriptionPlans.js'); if (plans) { const declared = new Set([...Object.keys(parseEnvKeys(read('.env.example'))), ...parseRenderKeys(read('render.yaml'))]); for (const m of plans.matchAll(/priceEnv:\s*'([A-Z0-9_]+)'/g)) { if (!declared.has(m[1])) { findings.push({ sev: 'error', area: 'payments', msg: `El plan de pago necesita ${m[1]} pero esa variable no está documentada en .env.example ni render.yaml — ese plan no se podrá cobrar.`, fixable: false }); } } }
 const usesStripe = walk('server', ['.js']).some(f => !DEV_FILE_RE.test(f) && /STRIPE_SECRET_KEY|require\(['"]stripe['"]\)/.test(read(f) || '')); const verifiesWebhook = walk('server', ['.js']).some(f => /constructEvent/.test(read(f) || '')); if (usesStripe && !verifiesWebhook) { findings.push({ sev: 'error', area: 'payments', msg: 'Usas Stripe pero ningún archivo verifica la firma del webhook (stripe.webhooks.constructEvent) — cualquiera podría falsificar un "pago exitoso".', fixable: false }); }
 return findings; }
function auditPhantomEndpoints() { const findings = []; const serverJs = read('server/server.js'); if (!serverJs) return findings; const registered = new Set(); for (const m of serverJs.matchAll(/app\.(?:use|get|post|put|patch|delete)\(\s*['""]\/api\/([a-zA-Z0-9-]+)/g)) registered.add(m[1]); const seen = new Map(); for (const f of walk('client/src', ['.js', '.jsx'])) { const c = read(f); if (!c) continue; for (const m of c.matchAll(/['""]\/api\/([a-zA-Z0-9-]+)/g)) { if (!seen.has(m[1])) seen.set(m[1], f); } } for (const [seg, file] of seen) { if (!registered.has(seg)) { findings.push({ sev: 'warn', area: 'client', msg: `El cliente llama a /api/${seg} (en ${file}) pero el servidor no tiene esa ruta registrada — esa llamada va a fallar.`, fixable: false }); } } return findings; }
function prodFiles(scope) { const dirs = scope === 'server' ? ['server'] : scope === 'client' ? ['client/src'] : ['server', 'client/src']; const out = []; for (const d of dirs) for (const f of walk(d, ['.js', '.jsx'])) if (!DEV_FILE_RE.test(f)) out.push(f); return out; }
function auditEngineering() { const findings = []; const importRe = /(import[^"']*?from|require\()\s*["'](\.[^"']+)["']/g; for (const f of prodFiles()) { const raw = read(f); if (!raw) continue; const dir = path.dirname(path.join(ROOT, f)); const c = raw.replace(/\/\*[\s\S]*?\*\//g, '').replace(/(^|[^:])\/\/[^\n]*/g, '$1'); for (const m of c.matchAll(importRe)) { const target = path.resolve(dir, m[2]); if (['', '.jsx', '.js', '.json', '.ts', '.tsx', '/index.jsx', '/index.js', '/index.ts'].some(e => fs.existsSync(target + e))) continue; const before = c.slice(Math.max(0, m.index - 20), m.index); if (/try\s*\{[^}]*$/.test(before)) continue; findings.push({ sev: m[1].startsWith('import') ? 'error' : 'warn', area: 'code', msg: m[1].startsWith('import') ? `${f}: importa "${m[2]}" pero ese archivo no existe — rompe el build y la sección no abrirá.` : `${f}: hace require("${m[2]}") y ese archivo no existe — truena cuando se ejecute esa parte.`, fixable: false }); } if (/\beval\s*\(/.test(c)) findings.push({ sev: 'error', area: 'security', msg: `${f}: usa eval() — permite ejecutar código arbitrario. Quítalo.`, fixable: false }); if (/(child_process|exec[A-Za-z]*)\s*\([^)]*(\$\{|\+\s*req\.)/.test(c)) findings.push({ sev: 'error', area: 'security', msg: `${f}: ejecuta comandos del sistema con datos variables — riesgo de inyección de comandos.`, fixable: false }); if (/console\.(log|error|info|warn)\([^)]*\b(req\.body\.password|user\.password|\.privateKey|process\.env\.(JWT_SECRET|STRIPE_SECRET_KEY|EMAIL_PASSWORD))\b/.test(c)) findings.push({ sev: 'warn', area: 'security', msg: `${f}: imprime datos sensibles (contraseña/secreto/clave) en consola — quítalo de los logs.`, fixable: false }); if (f.startsWith('client/') && /import\.meta\.env/.test(c)) findings.push({ sev: 'error', area: 'code', msg: `${f}: usa import.meta.env (sintaxis de Vite) pero el proyecto es Create React App — siempre será undefined y caerá al valor por defecto (localhost). Usa process.env.REACT_APP_*.`, fixable: false }); if (f.startsWith('client/') && /['"]https?:\/\/localhost/.test(c) && !/process\.env/.test(c)) findings.push({ sev: 'warn', area: 'code', msg: `${f}: usa una URL de localhost sin ninguna variable de entorno — en producción debe venir de process.env.REACT_APP_*.`, fixable: false }); } return findings; }
function auditHardening() { const findings = []; const s = read('server/server.js') || ''; const mids = [ { re: /helmet\s*\(/, name: 'helmet', desc: 'cabeceras de seguridad HTTP' }, { re: /mongoSanitize|mongo-sanitize/, name: 'express-mongo-sanitize', desc: 'previene inyección NoSQL' }, { re: /\bxss\s*\(/, name: 'xss-clean', desc: 'previene XSS' }, { re: /rateLimit|Limiter/, name: 'express-rate-limit', desc: 'frena fuerza bruta' } ]; for (const m of mids) if (!m.re.test(s)) findings.push({ sev: 'error', area: 'security', msg: `server.js no aplica ${m.name} (${m.desc}) — protección clave ausente.`, fixable: false }); if (/origin:\s*['"]\*['"]/.test(s) || /['"]Access-Control-Allow-Origin['"]\s*[:,]\s*['"]\*['"]/.test(s)) findings.push({ sev: 'error', area: 'security', msg: `server.js permite CORS desde cualquier origen ('*') — restríngelo a tu dominio.`, fixable: false }); const deps = loadDeps(); if (!deps.server.bcrypt && !deps.server.bcryptjs) findings.push({ sev: 'error', area: 'security', msg: `No hay bcrypt/bcryptjs instalado — verifica que las contraseñas NO se guarden en texto plano.`, fixable: false }); for (const f of prodFiles('server')) { const c = read(f); if (!c) continue; let idx = c.indexOf('jwt.sign('); while (idx !== -1) { const seg = c.slice(idx, idx + 220); if (!/expiresIn|exp\b/.test(seg)) { findings.push({ sev: 'warn', area: 'security', msg: `${f}: jwt.sign sin "expiresIn" — los tokens nunca caducan (si se roban, valen para siempre).`, fixable: false }); break; } idx = c.indexOf('jwt.sign(', idx + 1); } if (/\$where/.test(c)) findings.push({ sev: 'warn', area: 'security', msg: `${f}: usa $where en una consulta — vector de inyección NoSQL, evítalo.`, fixable: false }); if (/res\.[a-z]*\s*\(\s*[^)]*error\.stack/.test(c) || /\.json\(\s*err\s*\)/.test(c)) findings.push({ sev: 'warn', area: 'security', msg: `${f}: parece devolver el error/stack crudo al cliente — filtra detalles internos. Manda un mensaje genérico.`, fixable: false }); } return findings; }
function applyFixes(findings, dryRun) { const applied = []; const routeFixes = findings.filter(f => f.fixKind === 'register-route'); if (routeFixes.length) { let serverJs = read('server/server.js'); const anchor = "app.use('/api/health', require('./routes/health'));"; if (serverJs && serverJs.includes(anchor)) { let insertion = ''; for (const f of routeFixes) { const line = `app.use('/api/${f.route}', require('./routes/${f.route}')); // [auto] registrado por Kairos`; if (!serverJs.includes(`require('./routes/${f.route}')`)) insertion += '\n' + line; applied.push(`registrar ruta /api/${f.route}`); } if (insertion && !dryRun) { serverJs = serverJs.replace(anchor, anchor + insertion); fs.writeFileSync(path.join(ROOT, 'server/server.js'), serverJs); } } }
 const envFixes = findings.filter(f => f.fixKind === 'env-doc'); if (envFixes.length) { let envFile = read('.env.example') || ''; let block = '\n# ============ Añadidas por Kairos (usadas en código, sin documentar) ============\n'; let added = 0; for (const f of envFixes) { if (!new RegExp(`^${f.key}=`, 'm').test(envFile)) { block += `${f.key}=\n`; added++; applied.push(`documentar env ${f.key}`); } } if (added && !dryRun) fs.appendFileSync(path.join(ROOT, '.env.example'), block); } return applied; }
function pushTasks(findings) { let tasks = []; if (fs.existsSync(TASKS_FILE)) { try { tasks = JSON.parse(fs.readFileSync(TASKS_FILE, 'utf8')); } catch {} } const known = new Set(tasks.map(t => t.id)); let added = 0; for (const f of findings) { if (f.sev === 'info') continue; const id = `kairos-${f.area}-${(f.key || f.route || f.intg || f.msg).toString().toLowerCase().replace(/[^a-z0-9]+/g, '-').slice(0, 32)`; if (known.has(id)) continue; tasks.push({ id, type: f.sev === 'error' ? 'security' : 'improvement', priority: f.sev === 'error' ? 1 : 4, title: f.msg.slice(0, 90), description: f.msg, files: f.route ? [`server/routes/${f.route}.js`, 'server/server.js'] : [], acceptance: ['Resuelto y verificado en producción'], status: f.fixable ? 'pending' : 'needs_human', source: 'kairos', detectedAt: new Date().toISOString(), }); known.add(id); added++; } fs.writeFileSync(TASKS_FILE, JSON.stringify(tasks, null, 2)); return added; }
function printReport(report) { const icon = { error: '🔴', warn: '🟡', info: '🔵' }; const groups = { security: 'SEGURIDAD (modo pentester)', code: 'CÓDIGO (modo ingeniero)', deploy: 'DESPLIEGUE / WEB', payments: 'PAGOS (STRIPE)', env: 'VARIABLES DE ENTORNO', routes: 'CABLEADO DE RUTAS', integration: 'INTEGRACIONES', client: 'FRONTEND' }; console.log('\n╔════════════════════════════════════════════════════════╗'); console.log('║   KAIROS · ¿QUÉ LE FALTA A KRONOS PARA ESTAR EN LA WEB?  ║'); console.log('╚════════════════════════════════════════════════════════╝'); const counts = report.findings.reduce((a, f) => { a[f.sev] = (a[f.sev] || 0) + 1; return a; }, {}); console.log(`\n  🔴 Bloqueantes: ${counts.error || 0}   🟡 Pendientes: ${counts.warn || 0}   🔵 Notas: ${counts.info || 0}\n`); for (const [area, title] of Object.entries(groups)) { const items = report.findings.filter(f => f.area === area); if (!items.length) continue; console.log(`\n── ${title} ${'─'.repeat(Math.max(0, 40 - title.length))}`); items.sort((a, b) => ({ error: 0, warn: 1, info: 2 }[a.sev] - { error: 0, warn: 1, info: 2 }[b.sev])); for (const f of items) { console.log(`  ${icon[f.sev]} ${f.msg}${f.fixable ? '  ⟵ auto-fix disponible' : ''}`); } } if (!report.findings.length) console.log('\n  ✅ Sin gaps detectados. KRONOS parece listo para producción.\n'); console.log(''); }
function loadDeps() { const s = JSON.parse(read('server/package.json') || '{}'); const c = JSON.parse(read('client/package.json') || '{}'); return { server: { ...(s.dependencies || {}), ...(s.devDependencies || {}) }, client: { ...(c.dependencies || {}), ...(c.devDependencies || {}) }, }; }
function runStudy(options = {}) { const present = []; const missingStakes = []; for (const t of [{ cap: 'Feed de publicaciones', proof: 'server/routes/posts.js', nets: 'todas' }, { cap: 'Mensajería directa (DM)', proof: 'server/routes/messages.js', nets: 'todas' }, { cap: 'Chats grupales', proof: 'server/routes/groupChats.js', nets: 'WhatsApp, Discord' }, { cap: 'Stories 24h', proof: 'server/routes/ephemeralStories.js', nets: 'Instagram, Snapchat' }, { cap: 'Video (subida/streaming)', proof: 'server/routes/videos.js', nets: 'TikTok, YouTube' }, { cap: 'Comunidades / grupos', proof: 'server/routes/communities.js', nets: 'Reddit, Facebook' }, { cap: 'Notificaciones en tiempo real', proof: 'server/routes/notifications.js', nets: 'todas' }, { cap: 'Búsqueda', proof: 'server/routes/search.js', nets: 'todas' }, { cap: 'Recomendaciones / descubrir', proof: 'server/routes/recommendations.js', nets: 'TikTok, Instagram' }, { cap: 'Reportar / bloquear', proof: 'server/routes/reporting.js', nets: 'todas' }, { cap: 'Salas de audio en vivo', proof: 'server/routes/audio.js', nets: 'Clubhouse, X Spaces' }, { cap: 'Eventos', proof: 'server/routes/events.js', nets: 'Facebook, Meetup' }]) { (exists(t.proof) ? present : missingStakes).push(t); }
 const missingCommon = [{ cap: 'Encuestas / polls en posts', hint: 'X, Instagram', proof: null }, { cap: 'Reacciones múltiples (no solo like)', hint: 'Facebook, LinkedIn', proof: null }, { cap: 'Hashtags y temas en tendencia', hint: 'X, Instagram', proof: 'client/src/components/HashtagText.jsx' }, { cap: 'Guardar / colecciones (bookmarks)', hint: 'Instagram, Pinterest', proof: null }, { cap: 'Duetos / remix de video', hint: 'TikTok', proof: null }, { cap: 'Suscripción a creadores (paywall)', hint: 'Patreon, OnlyFans', proof: 'server/routes/subscription.js' }].filter(c => !c.proof || !exists(c.proof));
 const lines = [];
 const push = s => lines.push(s);
 push('# 🧠 KAIROS · Estudio comparativo de KRONOS vs las grandes redes'); push(`_Generado: ${new Date().toISOString()}_`); push(''); push('## 1. Qué tipo de producto es KRONOS'); push('No es "otra red social": es una **super-app** (social + comercio + delivery + wallet cripto/fiat + IA + AR), más cerca del modelo de WeChat que de Instagram. Esa combinación ES tu ventaja: nadie en occidente la tiene junta.'); push(''); push(`## 2. Funciones base que ya tienes (${present.length}/12)`); for (const t of present) push(`- ✅ ${t.cap}  _(como ${t.nets})_`); if (missingStakes.length) { push(''); push('### Funciones base que conviene revisar:'); for (const t of missingStakes) push(`- ⚠️ ${t.cap} — la tienen ${t.nets}. No encontré \\`${t.proof}\\`.`); } push(''); push('## 3. Detalles que otras redes tienen y a ti quizá te falten'); if (missingCommon.length) { for (const c of missingCommon) push(`- 💡 ${c.cap}  _(visto en ${c.hint})_`); } else { push('- ✅ Cubres incluso los extras comunes.'); } push(''); push('## 4. 🚀 Ideas para DESTACAR (apoyadas en lo que ya tienes)'); push('Ordenadas por impacto. Cada una se construye sobre modelos que ya existen en tu código:'); push(''); push(`### 1. Historias interactivas "elige tu aventura"`); push('- **La idea:** Stories ramificadas donde el espectador toca opciones y cambia el final. Ya tienes los nodos y el progreso modelados; falta el editor visual y el reproductor.'); push('- **Por qué resalta:** Ninguna red grande tiene narrativa ramificada nativa. Es contenido que engancha y se comparte solo.'); push('- **Ya tienes la base en:** `server/models/StoryNode.js`, `server/models/StoryProgress.js`'); push(''); push(`### 2. SocialFi: gana KRO por buen contenido + apuesta para impulsar`); push('- **La idea:** Recompensar con tokens KRO las publicaciones de calidad y dejar "stakear" KRO para impulsar un post o a un creador. Ya tienes pool de recompensas, staking y el token.'); push('- **Por qué resalta:** Mezcla economía real con red social — el creador gana de verdad, no solo "likes". WeChat/X tantean esto; tú ya tienes la base cripto.'); push('- **Ya tienes la base en:** `server/models/RewardPool.js`, `server/models/Stake.js`, `server/models/KronosToken.js`'); push(''); push(`### 3. Marketplace social con escrow (compra-venta segura entre usuarios)`); push('- **La idea:** Vender entre usuarios donde el dinero queda retenido (escrow) hasta que el comprador confirma — con reembolsos integrados. Ya tienes listings, refunds y wallet.'); push('- **Por qué resalta:** Facebook Marketplace no protege el pago; aquí la confianza es la función estrella. Diferenciador fuerte.'); push('- **Ya tienes la base en:** `server/models/Listing.js`, `server/models/Refund.js`, `server/models/CashWallet.js`'); push(''); push(`### 4. Estudio de creación con IA dentro de la app`); push('- **La idea:** Pestaña "Crear con IA": genera guiones, imágenes y videos sin salir de KRONOS, ligado a los planes que ya cobras (scripts/media).'); push('- **Por qué resalta:** Ninguna red junta creación con IA + monetización en un mismo flujo.'); push('- **Ya tienes la base en:** `server/routes/ai.js`, `server/services/aiService.js`, `server/config/subscriptionPlans.js`'); push(''); push(`### 5. Capa social en Realidad Aumentada + avatares 3D`); push('- **La idea:** Filtros/objetos AR, probarte items del marketplace en AR, y "drops" AR por ubicación que sueltan KRO al encontrarlos (estilo Pokémon GO).'); push('- **Por qué resalta:** Snapchat tiene AR pero no economía; tú puedes unir AR + token + comercio.'); push('- **Ya tienes la base en:** `server/routes/ar.js`, `server/models/AvatarItem.js`, `server/models/UserAvatar.js`'); push(''); push(`### 6. Move-to-earn: gana KRO por moverte`); push('- **La idea:** Conectar actividad física (pasos/ejercicio) con recompensas en KRO y retos sociales entre amigos. Ya registras salud.'); push('- **Por qué resalta:** StepN demostró que mueve millones; ninguna red social mainstream lo integra. Encaja con tu wallet.'); push('- **Ya tienes la base en:** `server/models/HealthLog.js`, `server/models/RewardPool.js`'); push(''); push(`### 7. Propinas y regalos en vivo con KRO`); push('- **La idea:** Regalar KRO en tiempo real durante salas de audio o video en vivo (como los "bits" de Twitch, pero con valor real y retirable).'); push('- **Por qué resalta:** Monetización directa creador-fan sin intermediario caro. Ya tienes propinas y salas en vivo.'); push('- **Ya tienes la base en:** `server/models/Tip.js`, `server/routes/audio.js`, `server/routes/videos.js`'); push(''); push('## 5. Recomendación de Kairos'); push('Tu mayor diferenciador NO es copiar funciones de otras redes — es **unir lo social con la economía KRO**. Prioriza: (1) SocialFi (ganar/impulsar con KRO), (2) Historias interactivas, (3) Marketplace con escrow. Son únicas y ya tienes los cimientos.'); push(''); const out = lines.join('\n'); console.log('\n' + out + '\n'); if (!options.reportOnly) { const file = path.join(__dirname, 'logs', 'kairos-study.md'); try { fs.mkdirSync(path.dirname(file), { recursive: true }); fs.writeFileSync(file, out); log(`Estudio guardado en ${path.relative(ROOT, file)}`); console.log(`  📄 Estudio guardado en agents/logs/kairos-study.md`); } catch {} } return { present, missingStakes, missingCommon, ideas: [] }; }
function isLiveProjectDir(abs) { const low = abs.toLowerCase(); return LIVE_DIRS.some(d => low === d || low.startsWith(d + path.sep)); }
function dirSize(abs) { let total = 0; let stack = [abs]; while (stack.length) { const d = stack.pop(); let entries; try { entries = fs.readdirSync(d, { withFileTypes: true }); } catch { continue; } for (const e of entries) { const full = path.join(d, e.name); if (e.isDirectory()) stack.push(full); else { try { total += fs.statSync(full).size; } catch {} } } } return total; }
function scanJunk(scanRoot) { const safe = []; const review = []; const stack = [scanRoot]; while (stack.length) { const dir = stack.pop(); let entries; try { entries = fs.readdirSync(dir, { withFileTypes: true }); } catch { continue; } for (const e of entries) { const full = path.join(dir, e.name); const isTopLevel = path.dirname(full) === scanRoot; if (e.isDirectory()) { if (CLEAN_SKIP_DIRS.has(e.name)) continue; if (APP_INSTALL_RE.test(e.name)) continue; if (JUNK_DIR_RE.test(e.name) && underRoot(full) && !isLiveProjectDir(full)) { safe.push({ abs: full, kind: 'dir', reason: `carpeta regenerable (${e.name})`, bytes: dirSize(full) }); continue; } if (REPLICA_RE.test(e.name) && (underRoot(full) || isTopLevel) && !isLiveProjectDir(full)) { safe.push({ abs: full, kind: 'dir', reason: 'réplica/versión vieja (carpeta)', bytes: dirSize(full) }); continue; } stack.push(full); continue; } let size = 0; try { size = fs.statSync(full).size; } catch {} if (JUNK_FILE_RE.test(e.name)) { safe.push({ abs: full, kind: 'file', reason: 'archivo basura del SO/editor', bytes: size }); } else if (REPLICA_RE.test(e.name) && (underRoot(full) || isTopLevel)) { safe.push({ abs: full, kind: 'file', reason: 'réplica/copia/versión vieja (por nombre)', bytes: size }); } } } return { safe, review }; }
function quarantine(items, scanRoot) { const ts = new Date().toISOString().replace(/[:.]/g, '-'); const trashBase = path.join(scanRoot, TRASH_DIRNAME, ts); const manifest = []; for (const it of items) { const relInside = path.relative(scanRoot, it.abs); const dest = path.join(trashBase, relInside); try { fs.mkdirSync(path.dirname(dest), { recursive: true }); fs.renameSync(it.abs, dest); manifest.push({ from: it.abs, to: dest, kind: it.kind, reason: it.reason }); } catch (err) { log(`No se pudo mover ${it.abs}: ${err.message}`); } } fs.mkdirSync(trashBase, { recursive: true }); fs.writeFileSync(path.join(trashBase, '_manifest.json'), JSON.stringify(manifest, null, 2)); return { trashBase, count: manifest.length }; }
function fmtBytes(n) { if (n > 1e9) return (n / 1e9).toFixed(1) + ' GB'; if (n > 1e6) return (n / 1e6).toFixed(1) + ' MB'; if (n > 1e3) return (n / 1e3).toFixed(1) + ' KB'; return n + ' B'; }
function runClean(options = {}) { const { apply = false, diskE = true } = options; const scanRoot = diskE ? DISK_E_ROOT : ROOT; log(`=== KAIROS modo verdugo — escaneando ${scanRoot} (apply=${apply}) ===`); console.log(`\n🧹 KAIROS limpieza · objetivo: ${scanRoot} · modo: ${apply ? 'CUARENTENA' : 'solo reporte'}\n`); const { safe, review } = scanJunk(scanRoot); const totalBytes = safe.reduce((a, b) => a + (b.bytes || 0), 0); if (safe.length === 0) { console.log('  ✨ Nada de basura encontrada. Disco limpio.'); } else { console.log(`  Encontré ${safe.length} elementos basura (${fmtBytes(totalBytes)} recuperables):\n`); for (const it of safe.slice(0, 40)) { console.log(`   ${it.kind === 'dir' ? '📁' : '📄'} ${path.relative(scanRoot, it.abs)}  — ${it.reason}`); } if (safe.length > 40) console.log(`   …y ${safe.length - 40} más (ver agents/logs/kairos-junk.json)`); } try { fs.mkdirSync(path.dirname(JUNK_FILE), { recursive: true }); fs.writeFileSync(JUNK_FILE, JSON.stringify({ generatedAt: new Date().toISOString(), scanRoot, safe, review }, null, 2)); } catch {} if (apply && safe.length) { const res = quarantine(safe, scanRoot); log(`Cuarentena: ${res.count} elementos movidos a ${res.trashBase}`); console.log(`\n  ✅ ${res.count} elementos movidos a cuarentena (reversibles):`); console.log(`     ${res.trashBase}`); console.log(`     Restaurar: revisa _manifest.json ahí. Borrar definitivo: elimina esa carpeta a mano.\n`); } else if (safe.length) { console.log(`\n  💡 Esto fue solo reporte. Para mover a cuarentena: node agents/kairos.js --clean --apply\n`); } log('=== KAIROS modo verdugo finalizado ==='); return { safe, review }; }
function audit() { log('=== KAIROS iniciando auditoría completa de KRONOS ==='); if (!exists('server') || !exists('client')) { return [{ sev: 'error', area: 'deploy', msg: 'Kairos no detecta las carpetas /server o /client. Asegúrate de ejecutar el agente desde la raíz del proyecto KRONOS.', fixable: false }]; } const findings = [ ...auditDeploy(), ...auditEnv(), ...auditRoutes(), ...auditIntegrations(), ...auditSecrets(), ...auditRouteGuards(), ...auditPayments(), ...auditPhantomEndpoints(), ...auditEngineering(), ...auditHardening(), ...auditClientPages(), ]; log(`Auditoría completada: ${findings.length} hallazgos`); return { generatedAt: new Date().toISOString(), findings }; }
function run(options = {}) { const { reportOnly = false, fix = false, json = false } = options; const report = audit(); if (json) { console.log(JSON.stringify(report, null, 2)); return report; } printReport(report); if (reportOnly) { log('Modo --report: no se escriben tareas ni fixes.'); return report; } try { fs.mkdirSync(path.dirname(REPORT_FILE), { recursive: true }); fs.writeFileSync(REPORT_FILE, JSON.stringify(report, null, 2)); } catch {} const added = pushTasks(report.findings); log(`${added} tareas nuevas encoladas en tasks.json para Builder Alpha / Pelos.`); console.log(`  📋 ${added} tareas nuevas añadidas a la cola (node agents/orchestrator.js --status para verlas)`); if (fix) { const applied = applyFixes(report.findings, false); if (applied.length) { log(`Auto-fixes aplicados: ${applied.join(', ')}`); console.log(`  🔧 Auto-fixes aplicados (${applied.length}): ${applied.join(', ')}`); } else { console.log('  🔧 No había auto-fixes seguros pendientes.'); } } else { const fixable = report.findings.filter(f => f.fixable).length; if (fixable) console.log(`  💡 ${fixable} hallazgos tienen auto-fix. Corre: node agents/kairos.js --fix`); } log('=== KAIROS finalizado ==='); return report; }
function runWatch() { console.log('👁️  KAIROS en modo vigilancia — revisa al guardar. Ctrl+C para salir.\n'); run({ reportOnly: true }); let timer = null; const trigger = () => { clearTimeout(timer); timer = setTimeout(() => { console.clear(); console.log('🔄 Cambio detectado — re-revisando...\n'); run({ reportOnly: true }); console.log('\n👁️  Vigilando... (Ctrl+C para salir)'); }, 600); }; for (const d of ['server', 'client/src', 'agents']) { const base = path.join(ROOT, d); if (!fs.existsSync(base)) continue; try { fs.watch(base, { recursive: true }, trigger); } catch {} } }
if (require.main === module) { const args = process.argv.slice(2); if (args.includes('--watch')) { runWatch(); } else if (args.includes('--study')) { runStudy({ reportOnly: args.includes('--report') }); } else if (args.includes('--clean')) { runClean({ apply: args.includes('--apply'), diskE: !args.includes('--solo-proyecto') }); } else { run({ reportOnly: args.includes('--report'), fix: args.includes('--fix'), json: args.includes('--json'), }); } } else { module.exports = { run, audit, runStudy, runClean }; }
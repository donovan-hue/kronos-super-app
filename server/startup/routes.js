module.exports = function configureRoutes(app) {
  // Aquí moveremos gradualmente todas las rutas:
  //
  // app.use('/api/auth', ...)
  // app.use('/api/posts', ...)
  // app.use('/api/users', ...)
  // app.use('/api/messages', ...)
  // etc.
  //
  return app;
};
